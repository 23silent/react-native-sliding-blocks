/**
 * Consolidated types for SlidingBlocks.
 * Re-exports from settings and layout for easier discovery.
 */

export type { GameLayout } from './layout'
export type {
  AnimationSettings,
  AppSettings,
  BlockSettings,
  CheckerboardSettings,
  ExplosionPresetsSettings,
  ExplosionSettings,
  FeedbackOpacitySettings,
  GameLayoutSettings
} from './settings'
